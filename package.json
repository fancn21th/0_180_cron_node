{
  "name": "cronjob",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cross-env ACTIVE_ENV=dev nodemon index.js",
    "dev": "cross-env ACTIVE_ENV=dev nodemon index.js",
    "staging": "cross-env ACTIVE_ENV=staging nodemon index.js",
    "prod": "cross-env ACTIVE_ENV=prod node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "node-cron": "^3.0.0",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.8"
  }
}
